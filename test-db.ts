import { prisma } from './src/lib/prisma'; async function run() { try { await prisma.user.findFirst({ where: { OR: [ { email: { equals: 'test@example.com', mode: 'insensitive' } }, { username: { equals: 'test', mode: 'insensitive' } } ] } }); console.log('success'); } catch(e) { console.error('ERROR NAME:', e.name); console.error('ERROR CODE:', e.code); console.error('ERROR META:', JSON.stringify(e.meta)); } } run();
